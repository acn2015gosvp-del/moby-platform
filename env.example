# ============================================
# MOBY Platform 환경 변수 설정
# ============================================
# 
# 사용 방법:
# 1. 이 파일을 .env로 복사하세요
#    Windows: copy env.example .env
#    Linux/Mac: cp env.example .env
# 2. 아래 값들을 실제 값으로 변경하세요
# 3. .env 파일은 Git에 커밋되지 않습니다 (보안)
# ============================================

# -------------------------------------------------------------------
# MQTT 설정
# -------------------------------------------------------------------
MQTT_HOST=localhost
MQTT_PORT=1883

# -------------------------------------------------------------------
# InfluxDB 설정
# -------------------------------------------------------------------
# 로컬 InfluxDB 사용 시
# INFLUX_URL=http://localhost:8086

# InfluxDB Cloud 사용 시
# InfluxDB 클라우드 URL
# 예시: https://us-east-1-1.aws.cloud2.influxdata.com
# ⚠️  아래 값은 예시입니다. 실제 값으로 변경하세요!
INFLUX_URL=https://[YOUR_REGION].cloud2.influxdata.com

# InfluxDB API 토큰 (읽기/쓰기 권한 필요)
# InfluxDB UI → Load Data → API Tokens에서 생성
# ⚠️  아래 값은 예시입니다. 실제 토큰으로 변경하세요!
INFLUX_TOKEN=your_influxdb_api_token_here_EXAMPLE_ONLY

# Organization 이름 또는 ID
# InfluxDB UI → Settings → Organization에서 확인
# ⚠️  아래 값은 예시입니다. 실제 Organization 이름으로 변경하세요!
INFLUX_ORG=your_organization_name

# Bucket 이름 (센서 데이터 저장소)
# InfluxDB UI → Load Data → Buckets에서 확인
# ⚠️  아래 값은 예시입니다. 실제 Bucket 이름으로 변경하세요!
INFLUX_BUCKET=your_bucket_name_for_sensors

# -------------------------------------------------------------------
# Grafana 설정 (필수 - 모니터링 페이지용)
# -------------------------------------------------------------------
# Grafana Base URL (대시보드 접근용)
# 예시: http://192.168.80.99:3000 또는 http://localhost:3000
# GRAFANA_URL=http://localhost:3000

# Grafana API Key (Grafana에서 생성한 API 토큰)
# 생성 방법: Grafana → Configuration → API Keys → New API Key
# 권한: Viewer 이상 필요 (대시보드 읽기)
# ⚠️  아래 값은 예시입니다. 실제 API 키로 변경하세요!
# GRAFANA_API_KEY=your-grafana-api-key-here

# Grafana Organization ID (기본값: 1)
# GRAFANA_ORG_ID=1

# Grafana 대시보드 UID (모니터링에 사용할 대시보드)
# Grafana 대시보드 설정에서 확인 가능
# GRAFANA_DASHBOARD_UID=conveyor-dashboard

# -------------------------------------------------------------------
# Gemini API 설정 (보고서 생성 및 알림 요약용)
# -------------------------------------------------------------------
# ⚠️  아래 값은 예시입니다. 실제 API 키로 변경하세요!
# Google AI Studio에서 발급: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# -------------------------------------------------------------------
# OpenAI API 설정 (사용 안 함 - Gemini API로 대체됨)
# -------------------------------------------------------------------
# OPENAI_API_KEY=your-openai-api-key-here

# -------------------------------------------------------------------
# 인증 설정 (필수 - 프로덕션 환경)
# -------------------------------------------------------------------
# SECRET_KEY는 프로덕션 환경에서 반드시 변경해야 합니다!
# 강력한 랜덤 문자열을 생성하여 사용하세요 (최소 32자 이상 권장)
SECRET_KEY=your-secret-key-here-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# -------------------------------------------------------------------
# 환경 설정
# -------------------------------------------------------------------
ENVIRONMENT=dev
# DEBUG=true
# LOG_LEVEL=INFO

