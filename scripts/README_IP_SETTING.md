# Windows IP 주소 고정 설정 가이드

## 📋 현재 네트워크 설정

- **네트워크 인터페이스**: Wi-Fi
- **현재 IP 주소**: 192.168.80.192
- **서브넷 마스크**: 255.255.255.0 (/24)
- **게이트웨이**: 192.168.80.1
- **DNS 서버**: 164.124.101.2, 8.8.8.8

## 🔧 IP 주소 고정 설정 방법

### 방법 1: PowerShell 스크립트 사용 (권장)

1. **PowerShell을 관리자 권한으로 실행**
   - 시작 메뉴에서 "PowerShell" 검색
   - 우클릭 → "관리자 권한으로 실행"

2. **스크립트 실행**
   ```powershell
   cd C:\Users\USER\projects\moby-platform\scripts
   .\set_static_ip.ps1
   ```

3. **설정 확인**
   - 스크립트가 자동으로 현재 설정을 확인하고 표시합니다.

### 방법 2: GUI를 통한 설정

1. **네트워크 설정 열기**
   - `Win + I` → 네트워크 및 인터넷 → Wi-Fi
   - 또는 제어판 → 네트워크 및 공유 센터

2. **어댑터 옵션 변경**
   - "어댑터 옵션 변경" 클릭
   - "Wi-Fi" 어댑터 우클릭 → 속성

3. **IPv4 속성 설정**
   - "Internet Protocol Version 4 (TCP/IPv4)" 선택 → 속성
   - "다음 IP 주소 사용" 선택
   - 다음 정보 입력:
     - IP 주소: `192.168.80.192`
     - 서브넷 마스크: `255.255.255.0`
     - 기본 게이트웨이: `192.168.80.1`
   - "다음 DNS 서버 주소 사용" 선택
     - 기본 설정 DNS 서버: `164.124.101.2`
     - 대체 DNS 서버: `8.8.8.8`
   - 확인 클릭

## 🔄 DHCP로 복원 (자동 IP 할당)

IP 주소를 다시 자동으로 할당받으려면:

```powershell
# 관리자 권한 PowerShell에서 실행
cd C:\Users\USER\projects\moby-platform\scripts
.\restore_dhcp_ip.ps1
```

또는 GUI에서:
- IPv4 속성 → "자동으로 IP 주소 받기" 선택

## ⚠️ 주의사항

1. **IP 주소 충돌**
   - 고정 IP 주소를 설정할 때는 라우터의 DHCP 범위와 겹치지 않도록 주의하세요.
   - 현재 IP (192.168.80.192)가 사용 가능한지 확인하세요.

2. **라우터 설정**
   - 라우터에서 해당 IP 주소를 DHCP 예약으로 설정하는 것이 더 안전합니다.
   - 이렇게 하면 IP 주소가 고정되면서도 DHCP의 다른 이점을 유지할 수 있습니다.

3. **네트워크 변경 시**
   - 다른 네트워크에 연결할 때는 DHCP로 복원하거나 새로운 네트워크에 맞는 IP를 설정해야 합니다.

## 🔍 현재 설정 확인

PowerShell에서 다음 명령어로 현재 설정을 확인할 수 있습니다:

```powershell
Get-NetIPConfiguration -InterfaceAlias "Wi-Fi"
```

## 📝 IP 주소 변경

다른 IP 주소로 변경하려면 `set_static_ip.ps1` 스크립트의 다음 변수를 수정하세요:

```powershell
$StaticIP = "192.168.80.XXX"  # 원하는 IP 주소
$Gateway = "192.168.80.1"      # 게이트웨이 (일반적으로 라우터 IP)
```

## 🆘 문제 해결

### 인터넷이 연결되지 않는 경우

1. **게이트웨이 확인**
   ```powershell
   ping 192.168.80.1
   ```

2. **DNS 서버 확인**
   ```powershell
   nslookup google.com
   ```

3. **라우터 설정 확인**
   - 라우터 관리 페이지 (일반적으로 192.168.80.1)에 접속하여 DHCP 범위 확인

### IP 주소 충돌 오류

- 다른 기기가 이미 해당 IP를 사용 중일 수 있습니다.
- 라우터의 DHCP 클라이언트 목록을 확인하세요.
- 다른 IP 주소를 사용하거나 DHCP 예약을 설정하세요.

